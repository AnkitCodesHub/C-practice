#include<stdio.h>
void printArray(int A, int n)
{
    for(int i=0; i<n;i++)
    {
        printf("%d",a[i])
    }
}

int main()
{
    int a[] = {12,54,65,7,23,9}
    int n = 6;
    printArray(a, n);
    return 0;
}